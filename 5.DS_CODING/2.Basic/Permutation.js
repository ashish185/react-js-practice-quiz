const getPermutation= (str) =>{
    if(str.length() === 0){
        const arr=[""];
        return arr;
    }
    let ch = str.charAt(0);
    let ros = str.substring(1)
}

const arr=[1,2];
console.log(arr.slice(0,0));

