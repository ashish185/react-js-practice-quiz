/**
 * * Function description will be in green
 * ! this can be only static
 * ? This is a question
 * todo: Pending task
 * @param {*} params 
 */
function static anyFunc(params) {
   //! comment color can be change
}