/* 
The Set object lets you store unique values of any type, whether primitive values or object references.
Limitation: Some of its methods are not compatible with this.

*/

//Instance properties: You don't need to call function over there.
st.size;

//Instance Methods
const st= new Set();
st.add();
st.clear();
st.delete();
st.entries();
st.forEach();
st.has();
st.keys();
st.values();

